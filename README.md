## 🟦 ১. Interface এবং type এর মধ্যে পার্থক্য।

### 🟩 Interface
- শুধুমাত্র object structure বর্ণনা করতে ব্যাবহার হয়.
- extend করা যায়.
- একই নামের interface একাধিকবার লিখলে merge হয়ে যায়.

### 🟩 type
- শুধু object  নয় primitive, function,union সব ধরনের টাইপ বর্ণনা করা যায়.
- extend করা যায়, কিন্তু merge হয় না.
- conditional type তৈরি করা যায়.

### 🟩 উদাহরণ

```ts
interface User{
    name:string;
    age:number;
};

type Products={
    title: string;
    price:number;
}

